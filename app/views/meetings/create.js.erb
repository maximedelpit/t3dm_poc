<% if @meeting.valid? %>
  <% if params[:meeting][:live] %>
    var conf_iframe = '<iframe src="https://appear.in/<%= j @meeting.room_name %>" class="responsive-img materialboxed" width="100%" height="600px" frameborder="0"></iframe>'
    $('#pending-call .conf-part').html(conf_iframe);
    $('#live-call').closeModal()
    $('#pending-call').openModal()
  <% else %>
    $('#modal1').closeModal();
    swal(
      'Success!',
      'Meeting created, an invitation has been sent!',
      'success'
    );
  <% end %>
<% else %>
  // TO DO
<% end %>

$('.datepicker').pickadate({
  selectMonths: true, // Creates a dropdown to control month
  selectYears: 1, // Creates a dropdown of 15 years to control year,
  autoclose: true
});
$('.clockpicker').clockpicker({
  autoclose: true
});

// TO DO INVESTIGATE TURBOLINKS
$(function() {
    Materialize.updateTextFields();
});
