<% if @topic.valid? %>
  $('#topic-form form')[0].reset()
  $('#topic-form-accordion .collapsible-header').click()
  $('#topic-wrapper').prepend("<%= j render 'topic_card', topic_hash: @gh_topic %>")
<% else %>
  $('#topic-form').fadeIn('<%= j render "new", topic: @topic%>' )
<% end %>
